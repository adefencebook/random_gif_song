<!doctype html>
  <head>
    <title> results_page </title>
    <link rel="stylesheet" type="text/css" href="css/style.css">
  </head>
  <body id="results_page_body">
    <% @tracks.each do |song| %>
  	   Song name: <%= song.title %> <br>
       <id="Artist_Picture">Artist Picture <br> <% song.artwork_url %> </id="Artist_Picture">
      <!-- song.artwork_url, song.stream_url, song.user.username -->
    <% end %>
  </body>
</html>
